FILE IS GENERATED BY WORKFLOW VALIDATION - DO NOT EDIT MANUALLY

# Workflow Validation Report
## Case: what-is-numpy-scipy
### File differences (if any)
- Missing generated files compared to expected:
  - python-numpy-introduction/resources/numpylogo.png
  - python-scipy-introduction/resources/scipy-logo.png
- Text files differ:
  - dependency_graph.md
  - proposed_structure.json
  - python-numpy-introduction/content.md
  - python-numpy-introduction/license.md
  - python-numpy-introduction/metadata.json
  - python-scipy-introduction/content.md
  - python-scipy-introduction/license.md
  - python-scipy-introduction/metadata.json
  - related_content_recommendations.md
### Qualitative difference assessment
- Learning objectives preserved: both runs split the single source into two focused introduction pages (NumPy, SciPy) and retain the core message — libraries let you reuse well-made code, and their C/C++/Fortran backends make them fast. Generated content is slightly more verbose and conversational; expected is more concise. Quality is comparable, neither a clear improvement nor regression.
- Prerequisite modelling diverges: expected makes SciPy depend on NumPy (yielding a `python_numpy_introduction --> python_scipy_introduction` edge in the graph), whereas the generated run treats both as peers depending only on `python-packages` and cross-links them via related content. Both are defensible; the generated "peer" choice is reasonable but drops the ordering the expected output encodes.
- License regression: the generated `license.md` emitted a short CC BY 4.0 notice, while expected uses the fuller CC BY-NC-SA 4.0 Imperial College license text. This is a meaningful, likely-incorrect divergence.
- Metadata inconsistencies: generated set `authors` to the display name "Chris Cooling" rather than the `chris-cooling` slug used elsewhere, and set `duration` to 5 vs expected 2. The author-slug mismatch is a probable regression.
- Content assets: expected pages open with the library logo images (`resources/*.png`) and use topic-style headings; the generated pages omitted the images entirely (missing resource files) and used question-style headings.
- Tags are consistent across both runs (`python` plus `numpy`/`scipy`), and the new `numpy`/`scipy` tags are correctly flagged for creation.

### Notable divergences
- `python-numpy-introduction/license.md` and `python-scipy-introduction/license.md`: generated CC BY 4.0 short form vs expected CC BY-NC-SA 4.0 full Imperial text — reviewer should confirm the correct license variant.
- `python-numpy-introduction/metadata.json` and `python-scipy-introduction/metadata.json`: `authors` uses "Chris Cooling" instead of the `chris-cooling` slug, and `duration` is 5 rather than 2.
- Missing resources: `python-numpy-introduction/resources/numpylogo.png` and `python-scipy-introduction/resources/scipy-logo.png` were not produced, and the generated `content.md` files dropped the logo image references present in expected.
- `proposed_structure.json` / `dependency_graph.md`: SciPy no longer lists `python-numpy-introduction` as a prerequisite (moved to related content), removing the numpy→scipy dependency edge.

